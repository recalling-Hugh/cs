import sys
n=int(sys.stdin.readline())
data=[]
score=[]
for i in range(n):
    data.append(list(map(int,sys.stdin.readline().split())))
    s=sum(data[i])
    score.append([s,data[i][0],n-i-1])
score.sort(reverse=True)
for i in range(min(n,5)):
    print(n-score[i][2],score[i][0])
